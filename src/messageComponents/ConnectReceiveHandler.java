package messageComponents;

import kademlia.Node;
import kademlia.Server;

public class ConnectReceiveHandler{
/*
    private final Server server;
    private final Node receivingNode;

    public ConnectReceiveHandler(Server server, Node node)
    {
        this.server = server;
        this.receivingNode = node;
    }

    public void receive(Message msg, int id) throws Exception
    {
        ConnectMessage cm = (ConnectMessage) msg;
        this.receivingNode.getRoutingTable().addContact(cm.getOriginNode());

        AckMessage ack = new AckMessage(this.receivingNode);

        // Server should reply with the ack below.
     }

 */
}
